const path=require('path'),fs=require('fs'),{pathToFileURL}=require('url'),{spawnSync}=require('child_process');const root=__dirname,frames=path.join(root,'frames');fs.mkdirSync(frames,{recursive:true});const chrome='/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',url=pathToFileURL(path.join(root,'reel.html')).href;const requested=Number(process.argv[2]);const scenes=Number.isInteger(requested)&&requested>=1&&requested<=6?[requested]:[1,2,3,4,5,6];for(const n of scenes){const file=path.join(frames,`scene-${n}.png`);const run=spawnSync(chrome,['--headless','--disable-gpu','--hide-scrollbars','--run-all-compositor-stages-before-draw','--virtual-time-budget=1200','--window-size=1080,1920',`--screenshot=${file}`,`${url}?scene=${n}`],{stdio:'inherit'});if(run.status!==0)process.exit(run.status||1)}
